## Оценка
Ни одно решение не доказано.

## Выбор
приемлемого нет: ни в одном ответе нет запущенной проверки или цитаты кода.

```json
{"choice": "none", "must_do": [], "feedback": {"A": "нет улики, что refund() вызывается только из api.py", "B": "тест не назван", "C": "assert без вызывающих мест"}}
```
